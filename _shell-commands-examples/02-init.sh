keycard-select
# Set pin, puk, pairing password (pairing password defaults to
# KeycardDefaultPairing if omitted — useful for V1 dev cards)
keycard-set-secrets 123456 123456789012
# or generate random secrets (omit keycard-set-secrets entirely)
keycard-init
